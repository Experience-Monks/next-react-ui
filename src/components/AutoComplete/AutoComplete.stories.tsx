import AutoComplete, { Props } from './AutoComplete';

export default { title: 'components/AutoComplete' };

export const Default = (args: Props) => <AutoComplete {...args} />;

Default.args = {
  // add control data
};
